{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "title": "DVA Configuration Schema",
  "description": "Schema for the dva.yml configuration file",
  "type": "object",
  "additionalProperties": false,
  "definitions": {
    "remote_secrets": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "sources": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "sops": {
                "type": "string",
                "minLength": 1
              }
            },
            "required": [
              "sops"
            ]
          }
        },
        "targets": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "provider": {
                "enum": [
                  "github-actions"
                ]
              },
              "repository": {
                "type": "string",
                "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*/[A-Za-z0-9][A-Za-z0-9_.-]*$",
                "maxLength": 200
              },
              "source": {
                "type": "string",
                "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*$"
              },
              "keys": {
                "type": "object",
                "additionalProperties": {
                  "type": "string"
                },
                "minProperties": 1,
                "maxProperties": 64
              }
            },
            "required": [
              "provider",
              "repository",
              "source",
              "keys"
            ]
          }
        }
      },
      "required": [
        "sources",
        "targets"
      ]
    },
    "remote_job": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "provider": {
          "enum": [
            "github-actions"
          ]
        },
        "repository": {
          "type": "string",
          "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*/[A-Za-z0-9][A-Za-z0-9_.-]*$",
          "maxLength": 200
        },
        "ref": {
          "type": "string",
          "minLength": 1,
          "maxLength": 255
        },
        "timeout": {
          "type": "string"
        },
        "inputs": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "default": {
                "type": "string",
                "maxLength": 4096
              },
              "required": {
                "type": "boolean"
              },
              "values": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "maxItems": 64,
                "uniqueItems": true
              }
            }
          },
          "maxProperties": 25
        },
        "secret_targets": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*$"
          },
          "maxItems": 64,
          "uniqueItems": true
        },
        "runs": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "name": {
                "type": "string",
                "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*$"
              },
              "workflow": {
                "type": "string",
                "pattern": "^[A-Za-z0-9_][A-Za-z0-9_.-]*\\.ya?ml$"
              },
              "inputs": {
                "type": "object",
                "additionalProperties": {
                  "type": "string",
                  "maxLength": 4096
                },
                "maxProperties": 25
              },
              "images": {
                "type": "array",
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "reference": {
                      "type": "string",
                      "minLength": 1,
                      "maxLength": 4096
                    },
                    "platforms": {
                      "type": "array",
                      "items": {
                        "type": "string"
                      },
                      "maxItems": 16,
                      "uniqueItems": true
                    }
                  },
                  "required": [
                    "reference"
                  ]
                },
                "maxItems": 16
              },
              "result_artifact": {
                "type": "string",
                "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*$"
              }
            },
            "required": [
              "name",
              "workflow"
            ]
          },
          "minItems": 1,
          "maxItems": 16
        }
      },
      "required": [
        "provider",
        "repository",
        "ref",
        "timeout",
        "runs"
      ]
    },
    "environment_vars": {
      "type": "object",
      "description": "Defines environment variables",
      "additionalProperties": {
        "type": ["string", "boolean", "number"]
      },
      "examples": [
        { "RAILS_ENV": "development" },
        { "DATABASE_URL": "postgres://user:password@db:5432/myapp_development" },
        { "PORT": "${PORT:-3000}" }
      ]
    },
    "service_tag_config": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "tags": { "type": "array", "items": { "type": "string" } }
      }
    },
    "compose_runner_config": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "files": { "type": "array", "items": { "type": "string" } },
        "project_name": { "type": "string" },
        "command": { "type": "string" },
        "method": { "type": "string" },
        "up_options": { "type": "array", "items": { "type": "string" } },
        "tags": { "type": "array", "items": { "type": "string" } },
        "services": {
          "type": "object",
          "patternProperties": {
            "^[\\w\\-.]+$": { "$ref": "#/definitions/service_tag_config" }
          },
          "additionalProperties": false
        }
      }
    },
    "native_runner_config": {
      "type": "object",
      "description": "Native (host process) runner configuration",
      "additionalProperties": false,
      "properties": {
        "dir": { "type": "string", "description": "Working directory" },
        "build": { "type": "string", "description": "Build command" },
        "post_build": { "type": "string", "description": "Post-build shell command (runs after build)" },
        "run": { "type": "string", "description": "Run command" },
        "env": { "type": "object", "additionalProperties": { "type": "string" } }
      }
    },
    "docker_runner_config": {
      "type": "object",
      "description": "Docker runner configuration (application-style: image/run/build/command)",
      "additionalProperties": false,
      "properties": {
        "image": { "type": "string" },
        "run": { "type": "string", "description": "Full docker run command" },
        "build": { "type": "string", "description": "Image build command" },
        "command": { "type": "string", "description": "Command passed to the container" },
        "ports": { "type": "array", "items": { "type": "string" } },
        "volumes": { "type": "array", "items": { "type": "string" } },
        "env": { "type": "object", "additionalProperties": { "type": "string" } },
        "options": { "type": "array", "items": { "type": "string" } }
      }
    },
    "process_runner_config": {
      "type": "object",
      "description": "Local process runner configuration",
      "additionalProperties": false,
      "properties": {
        "command": { "type": "string", "description": "Command to run" },
        "dir": { "type": "string", "description": "Working directory" },
        "ready_timeout": { "type": "integer", "description": "Timeout in seconds to wait for readiness" }
      }
    },
    "script_runner_config": {
      "type": "object",
      "description": "Shell script runner configuration",
      "additionalProperties": false,
      "properties": {
        "up": { "type": "string", "description": "Script to run on up" },
        "down": { "type": "string", "description": "Script to run on down" },
        "stop": { "type": "string", "description": "Script to run on stop" }
      }
    },
    "kubectl_runner_config": {
      "type": "object",
      "description": "Kubectl apply runner configuration",
      "additionalProperties": false,
      "properties": {
        "manifests": { "type": "array", "items": { "type": "string" } },
        "namespace": { "type": "string" },
        "context": { "type": "string" },
        "kubeconfig": { "type": "string", "description": "Path to kubeconfig file" }
      }
    },
    "helm_runner_config": {
      "type": "object",
      "description": "Helm chart deployment runner configuration",
      "additionalProperties": false,
      "properties": {
        "chart": { "type": "string" },
        "release": { "type": "string" },
        "namespace": { "type": "string" },
        "context": { "type": "string" },
        "values": { "type": "array", "items": { "type": "string" } },
        "set": { "type": "object", "additionalProperties": { "type": "string" } }
      }
    },
    "kustomize_runner_config": {
      "type": "object",
      "description": "Kustomize overlay runner configuration",
      "additionalProperties": false,
      "properties": {
        "dir": { "type": "string" },
        "namespace": { "type": "string" },
        "context": { "type": "string" }
      }
    },
    "tilt_runner_config": {
      "type": "object",
      "description": "Tilt local dev runner configuration",
      "additionalProperties": false,
      "properties": {
        "dir": { "type": "string" },
        "args": { "type": "array", "items": { "type": "string" } }
      }
    },
    "skaffold_runner_config": {
      "type": "object",
      "description": "Skaffold pipeline runner configuration",
      "additionalProperties": false,
      "properties": {
        "config": { "type": "string" },
        "profile": { "type": "string" },
        "args": { "type": "array", "items": { "type": "string" } }
      }
    },
    "podman_compose_runner_config": {
      "type": "object",
      "description": "Podman Compose runner configuration",
      "additionalProperties": false,
      "properties": {
        "files": { "type": "array", "items": { "type": "string" } },
        "project_name": { "type": "string" }
      }
    },
    "vagrant_runner_config": {
      "type": "object",
      "description": "Vagrant VM runner configuration",
      "additionalProperties": false,
      "properties": {
        "dir": { "type": "string" },
        "machine": { "type": "string" }
      }
    },
    "sam_runner_config": {
      "type": "object",
      "description": "AWS SAM local runner configuration",
      "additionalProperties": false,
      "properties": {
        "template": { "type": "string" },
        "port": { "type": "integer" },
        "args": { "type": "array", "items": { "type": "string" } }
      }
    },
    "serverless_runner_config": {
      "type": "object",
      "description": "Serverless offline runner configuration",
      "additionalProperties": false,
      "properties": {
        "dir": { "type": "string" },
        "port": { "type": "integer" },
        "args": { "type": "array", "items": { "type": "string" } }
      }
    },
    "multipass_runner_config": {
      "type": "object",
      "description": "Multipass VM runner configuration",
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "image": { "type": "string" },
        "cpus": { "type": "integer" },
        "memory": { "type": "string" },
        "disk": { "type": "string" },
        "cloud_init": { "type": "string" }
      }
    },
    "env_file": {
      "oneOf": [
        {
          "type": "string",
          "description": "Path to a single .env file"
        },
        {
          "type": "array",
          "items": {
            "oneOf": [
              { "type": "string" },
              {
                "type": "object",
                "properties": {
                  "path": { "type": "string" },
                  "required": { "type": "boolean", "default": false },
                  "sops_source": {
                    "type": "string",
                    "description": "Encrypted file that produces this target, unsealed on demand by `dva config env unseal`. Declaration metadata only: it never affects loading, never triggers decryption, and is accepted only in the top-level env_file",
                    "examples": ["secrets.env.enc"]
                  }
                },
                "required": ["path"],
                "additionalProperties": false
              }
            ]
          },
          "description": "Array of .env file paths (later files override earlier)"
        },
        {
          "type": "object",
          "properties": {
            "files": {
              "oneOf": [
                { "type": "string" },
                {
                  "type": "array",
                  "items": {
                    "oneOf": [
                      { "type": "string" },
                      {
                        "type": "object",
                        "properties": {
                          "path": { "type": "string" },
                          "required": { "type": "boolean", "default": false },
                          "sops_source": {
                            "type": "string",
                            "description": "Encrypted file that produces this target, unsealed on demand by `dva config env unseal`. Declaration metadata only: it never affects loading, never triggers decryption, and is accepted only in the top-level env_file",
                            "examples": ["secrets.env.enc"]
                          }
                        },
                        "required": ["path"],
                        "additionalProperties": false
                      }
                    ]
                  }
                }
              ]
            },
            "required": {
              "type": "boolean",
              "default": false,
              "description": "Whether env files are required to exist"
            }
          },
          "required": ["files"],
          "additionalProperties": false
        }
      ],
      "examples": [
        ".env",
        [".env.defaults", ".env", ".env.local"],
        {
          "files": [".env.defaults", ".env"],
          "required": false
        }
      ]
    },
    "provision_item": {
      "oneOf": [
        {
          "type": "string",
          "description": "Raw shell command string"
        },
        {
          "type": "object",
          "description": "Step-based command with name, run commands, and optional notes",
          "additionalProperties": false,
          "properties": {
            "step": {
              "type": "string",
              "description": "Name of the step (displayed with progress indicator)"
            },
            "run": {
              "oneOf": [
                { "type": "string" },
                { "type": "array", "items": { "type": "string" } }
              ],
              "description": "Command(s) to execute for this step"
            },
            "note": {
              "type": "string",
              "description": "Informational note displayed before/after commands"
            },
            "compose_up": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Docker Compose services to start (inherits compose config)"
            },
            "compose_exec": {
              "type": "string",
              "description": "Command to execute in a compose service"
            },
            "compose_run": {
              "type": "string",
              "description": "One-off command in a compose service"
            },
            "parallel": {
              "type": "boolean",
              "description": "Run this step concurrently with other consecutive parallel steps. Honoured under 'provision:' only — interaction steps and hooks always run sequentially, and 'dva validate' warns when the key appears there.",
              "default": false
            }
          },
          "anyOf": [
            { "required": ["step"] },
            { "required": ["run"] },
            { "required": ["note"] },
            { "required": ["compose_up"] },
            { "required": ["compose_exec"] },
            { "required": ["compose_run"] }
          ]
        },
        {
          "type": "object",
          "description": "Legacy structured command (echo/cmd only; use raw strings for sleep/shell)",
          "additionalProperties": false,
          "properties": {
            "echo": { "type": "string" },
            "cmd": { "type": "string" }
          },
          "minProperties": 1,
          "maxProperties": 1
        }
      ]
    },
    "interaction_command": {
      "type": "object",
      "description": "Configuration for an interaction command",
      "additionalProperties": false,
      "properties": {
        "description": {
          "type": "string",
          "description": "Describes the command",
          "examples": ["Run Rails commands", "Connect to PostgreSQL database"]
        },
        "service": {
          "type": "string",
          "description": "Specifies the service associated with the command",
          "examples": ["web", "frontend", "db"]
        },
        "command": {
          "oneOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "string" }, "minItems": 1 }
          ],
          "description": "Command to execute — a string, or a list run sequentially",
          "examples": ["bundle exec rails", "npm", ["pnpm install", "pnpm build"]]
        },
        "script": {
          "type": "string",
          "description": "Inline shell script, usually a block scalar. Execution precedence: steps > script_file > script > command",
          "examples": ["set -e\nmake build\nmake test\n"]
        },
        "script_file": {
          "type": "string",
          "description": "Path to an external shell script, relative to dva.yml. Wins over 'script'",
          "examples": ["scripts/seed.sh"]
        },
        "steps": {
          "type": "array",
          "items": { "$ref": "#/definitions/provision_item" },
          "description": "Named steps run sequentially with progress output. Wins over every other execution field",
          "minItems": 1
        },
        "workdir": {
          "type": "string",
          "description": "Specifies the working directory for the command",
          "examples": ["/workdir", "/app"]
        },
        "user": {
          "type": "string",
          "description": "Specifies the user to run the command as",
          "examples": ["root:root", "root", "www-data", "1000:1000"]
        },
        "default_args": {
          "type": "string",
          "description": "Default arguments, used when the invocation supplies none. Inherited by a subcommand only while that subcommand does not redeclare what it runs — command, script, script_file or steps: a child's declaration replaces the parent's rather than extending it, so its arguments start empty. A container subcommand that declares only a description still inherits both. See USAGE.md, interaction.subcommands.",
          "examples": ["server -p 3000 -b 0.0.0.0"]
        },
        "environment": {
          "$ref": "#/definitions/environment_vars"
        },
        "compose": {
          "type": "object",
          "description": "Allows specifying Docker Compose options",
          "additionalProperties": false,
          "properties": {
            "method": {
              "type": "string",
              "description": "Specifies the Docker Compose method (e.g., up, run)",
              "examples": ["run", "up", "exec"]
            },
            "compose_method": {
              "type": "string",
              "description": "Specifies an alternative Docker Compose method to use in compose commands",
              "examples": ["up"]
            },
            "run_options": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Options to pass to the 'docker-compose run' command",
              "examples": [["service-ports", "rm"]]
            },
            "profiles": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Docker Compose profiles to use",
              "examples": [["web", "development"], ["frontend"], ["test"]]
            }
          }
        },
        "shell": {
          "type": "boolean",
          "description": "Enables or disables shell interpolation"
        },
        "destructive": {
          "type": "boolean",
          "description": "Marks this command as destructive (e.g. database reset, cache flush). When true, interactive execution requires confirmation unless --yes is supplied, and 'dva agent-deny install --scope project' projects deny rules into agent runtime settings"
        },
        "entrypoint": {
          "type": "string",
          "description": "Specifies the command entrypoint"
        },
        "runner": {
          "type": "string",
          "description": "Specifies the runner (e.g., docker_compose, kubectl)"
        },
        "pod": {
          "type": "string",
          "description": "Specifies the pod for the kubectl runner"
        },
        "subcommands": {
          "type": "object",
          "description": "Contains subcommands with the same structure as main commands",
          "patternProperties": {
            "^[\\w\\-.:/\\s]+$": {
              "$ref": "#/definitions/interaction_command"
            }
          },
          "minProperties": 1,
          "additionalProperties": false
        },
        "tags": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Tags for categorizing this command (e.g., test, quality, infra)",
          "examples": [["test"], ["quality"], ["infra", "db"]]
        },
        "before": {
          "type": "array",
          "items": { "$ref": "#/definitions/provision_item" },
          "description": "Steps to run before a hookable built-in command (build, down, logs, restart, stop, up)"
        },
        "replace": {
          "type": "array",
          "items": { "$ref": "#/definitions/provision_item" },
          "description": "Steps that replace the built-in command entirely"
        },
        "after": {
          "type": "array",
          "items": { "$ref": "#/definitions/provision_item" },
          "description": "Steps to run after the built-in command completes successfully"
        }
      }
    }
  },
  "properties": {
    "secrets": { "$ref": "#/definitions/remote_secrets" },
    "jobs": { "type": "object", "additionalProperties": { "$ref": "#/definitions/remote_job" } },
    "version": {
      "type": "string",
      "description": "Optional. Specifies the minimum version of DVA this config requires of its reader — not the version of the binary that wrote it. Must not exceed the installed DVA version, or the config is rejected. Omit it for no compatibility gate.",
      "pattern": "^v?\\d+\\.\\d+(\\.\\d+)?$",
      "examples": ["0.1.0"]
    },
    "vars": {
      "type": "object",
      "description": "Global variables injected into all execution contexts. Merged with environment, site, and plan vars at runtime. Priority (lowest to highest): env_file < global vars < environment vars < site vars < plan vars < CLI vars < OS environment variables. An OS environment variable always wins: no dva.yml layer (not even --var) can override a key that is set in the OS environment.",
      "additionalProperties": { "type": ["string", "boolean", "number"] },
      "examples": [{ "LOG_FORMAT": "text", "APP_ENV": "dev" }]
    },
    "plans": {
      "type": "object",
      "description": "Named execution plans. Each plan combines stack entries, environment, site, and vars into an executable configuration. Use 'dva up <plan-name>' to execute.",
      "patternProperties": {
        "^[\\w\\-.]+$": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "description": { "type": "string", "description": "Human-readable description of this plan" },
            "alias": {
              "type": "string",
              "description": "Optional reference to another plan by name. When set, this plan becomes an alias for the target plan. Mutually exclusive with all other fields except description — it has no entries, vars, environment, site, endpoint_tags, or composes of its own."
            },
            "extends": {
              "type": "string",
              "description": "Optional reference to a single parent plan by name. When set, this plan inherits from the parent and can override fields. Mutually exclusive with alias and composes. The parent must be a concrete plan (not an alias or composition plan). Merge rules: scalar fields (description, environment, site, endpoint_tags) are overridden by child; vars is key-merged; entries are matched by name — a child entry with the same name replaces the parent entry entirely (no services union); new entries are appended."
            },
            "environment": { "type": "string", "description": "Environment name to apply (references environments section)" },
            "site": { "type": "string", "description": "Site name to apply (references sites section)" },
            "endpoint_tags": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Show only endpoints matching these tags after successful startup. Omit or use an empty list to show all endpoints."
            },
            "vars": {
              "type": "object",
              "description": "Plan-level variables (highest priority after CLI override)",
              "additionalProperties": { "type": ["string", "boolean", "number"] }
            },
            "entries": {
              "type": "array",
              "description": "Ordered list of stack entry references with execution parameters",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "name": { "type": "string", "description": "Stack entry name to reference" },
                  "runner": { "type": "string", "description": "Runner to use from the stack entry's runners map" },
                  "order": { "type": "integer", "description": "Execution order within this plan" },
                  "depends_on": { "type": "array", "items": { "type": "string" }, "description": "Names of other plan entries that must complete first" },
                  "profiles": { "type": "array", "items": { "type": "string" }, "description": "Compose profiles to activate for this entry, passed as --profile flags (compose runner only). Profiles decide which services compose considers; 'services' then narrows that set. Omit to leave every profile off, which is docker's default.", "examples": [["rust"], ["rust", "monitoring"]] },
                  "services": { "type": "array", "items": { "type": "string" }, "description": "Compose service subset to start (compose runner only)" },
                  "vars": {
                    "type": "object",
                    "description": "Entry-level additional variables",
                    "additionalProperties": { "type": ["string", "boolean", "number"] }
                  }
                },
                "required": ["name"]
              }
            },
            "composes": {
              "type": "array",
              "description": "Ordered list of composed plan references (root-aggregation composition, TASK-260 §3). Mutually exclusive with 'entries' and 'extends' on the same plan — a composition plan has no stack entries of its own and cannot declare environment/site/top-level vars.",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "plan": { "type": "string", "description": "Local leaf plan name, or an already-imported canonical/alias plan name (subprojects.<name>.import.plans)" },
                  "order": { "type": "integer", "description": "Execution order within this composition" },
                  "depends_on": { "type": "array", "items": { "type": "string" }, "description": "Other composes[].plan values in this same list that must complete first" },
                  "vars": {
                    "type": "object",
                    "description": "Override applied to this composed plan invocation only",
                    "additionalProperties": { "type": ["string", "boolean", "number"] }
                  }
                },
                "required": ["plan"]
              }
            }
          },
          "examples": [{
            "local-dev": {
              "description": "Local development plan",
              "environment": "dev",
              "site": "local",
              "vars": { "LOG_LEVEL": "debug" },
              "entries": [
                { "name": "core-compose", "runner": "compose", "order": 10, "profiles": ["monitoring"], "services": ["postgres", "redis"] },
                { "name": "api", "runner": "native", "order": 20, "depends_on": ["core-compose"] }
              ]
            },
            "hybrid": {
              "alias": "local-dev",
              "description": "Alias for local-dev"
            },
            "infra-full": {
              "extends": "local-infra",
              "description": "Extended plan with additional services",
              "entries": [
                { "name": "core-compose", "runner": "compose", "order": 10, "services": ["postgres", "redis", "minio", "grafana"] }
              ]
            }
          }]
        }
      },
      "additionalProperties": false
    },
    "sites": {
      "type": "object",
      "description": "Named host-based execution conditions. Sites represent where DVA runs (local, office, remote, cloud), not the deployment target.",
      "patternProperties": {
        "^[\\w\\-.]+$": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "description": { "type": "string" },
            "vars": {
              "type": "object",
              "description": "Site-level variables",
              "additionalProperties": { "type": ["string", "boolean", "number"] }
            },
            "entry_overrides": {
              "type": "object",
              "description": "Per-stack-entry overrides for this site",
              "patternProperties": {
                "^[\\w\\-.]+$": {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "runner": { "type": "string", "description": "Override runner selection for this stack entry" },
                    "vars": {
                      "type": "object",
                      "description": "Additional variables for this entry in this site",
                      "additionalProperties": { "type": ["string", "boolean", "number"] }
                    }
                  }
                }
              },
              "additionalProperties": false
            }
          },
          "examples": [{
            "local": { "vars": { "DVA_SITE": "local" } },
            "remote": {
              "vars": { "DVA_SITE": "remote" },
              "entry_overrides": {
                "api": { "runner": "docker", "vars": { "API_URL": "remote-api" } }
              }
            }
          }]
        }
      },
      "additionalProperties": false
    },
    "stack": {
      "type": "object",
      "description": "Named stack declarations. Compose must be declared through stack.<entry>.runners.compose; legacy stack.<entry>.compose and plugin: compose shapes are rejected by schema validation.",
      "allOf": [
        {
          "properties": {
            "compose": {
              "not": {
                "anyOf": [
                  { "required": ["command"] },
                  { "required": ["method"] },
                  { "required": ["files"] },
                  { "required": ["project_name"] },
                  { "required": ["up_options"] },
                  { "required": ["services"] }
                ]
              }
            }
          }
        }
      ],
      "additionalProperties": {
        "type": "object",
        "additionalProperties": true,
        "not": {
          "anyOf": [
            { "required": ["compose"] },
            { "properties": { "plugin": { "enum": ["compose"] } }, "required": ["plugin"] },
            {
              "required": ["runners"],
              "properties": { "runners": { "required": ["compose"] } },
              "not": { "required": ["default_runner"] }
            },
            { "required": ["files"] },
            { "required": ["project_name"] },
            { "required": ["up_options"] },
            { "required": ["services"] }
          ]
        },
        "properties": {
          "default_runner": {
            "type": "string",
            "description": "Default runner name when not specified in plan entry. Must reference a key in the runners map."
          },
          "description": {
            "type": "string",
            "description": "Human-readable description of this stack declaration"
          },
          "runners": {
            "type": "object",
            "description": "Map of runner name to runner configuration. Keys must be one of the runner names the config decoder accepts (see decodeRunnerNode in internal/config/lifecycle.go). Unknown runner names and unknown keys inside a runner are rejected.",
            "properties": {
              "native": { "$ref": "#/definitions/native_runner_config" },
              "docker": { "$ref": "#/definitions/docker_runner_config" },
              "compose": { "$ref": "#/definitions/compose_runner_config" },
              "process": { "$ref": "#/definitions/process_runner_config" },
              "script": { "$ref": "#/definitions/script_runner_config" },
              "kubectl": { "$ref": "#/definitions/kubectl_runner_config" },
              "helm": { "$ref": "#/definitions/helm_runner_config" },
              "kustomize": { "$ref": "#/definitions/kustomize_runner_config" },
              "tilt": { "$ref": "#/definitions/tilt_runner_config" },
              "skaffold": { "$ref": "#/definitions/skaffold_runner_config" },
              "podman-compose": { "$ref": "#/definitions/podman_compose_runner_config" },
              "podman_compose": { "$ref": "#/definitions/podman_compose_runner_config" },
              "vagrant": { "$ref": "#/definitions/vagrant_runner_config" },
              "sam": { "$ref": "#/definitions/sam_runner_config" },
              "serverless": { "$ref": "#/definitions/serverless_runner_config" },
              "multipass": { "$ref": "#/definitions/multipass_runner_config" }
            },
            "additionalProperties": false,
            "examples": [{
              "native": { "dir": "apps/api", "run": "go run ./cmd/api" },
              "docker": { "image": "myorg/api:dev" },
              "compose": { "files": ["docker-compose.yml"] }
            }]
          },
          "vars": {
            "type": "object",
            "description": "Stack-entry-level variables",
            "additionalProperties": { "type": ["string", "boolean", "number"] }
          },
          "plugin": {
            "type": "string",
            "description": "Legacy flat plugin type for non-compose plugins. Compose declarations must use runners.compose.",
            "enum": ["process", "script", "docker", "kubectl", "helm", "kustomize", "tilt", "skaffold", "podman-compose", "podman_compose", "vagrant", "sam", "serverless", "multipass"],
            "examples": ["kubectl", "helm"]
          },
          "order": {
            "type": "integer",
            "description": "Execution order (ascending for up, descending for down/stop)",
            "examples": [10, 20, 30]
          },
          "source": {
            "type": "object",
            "description": "Externally-owned stack fetched (git) or referenced (path) before this entry's runner runs. Exactly one of git/path. (TASK-051)",
            "properties": {
              "git": { "type": "string", "description": "Git repository URL cloned into the source cache" },
              "ref": { "type": "string", "description": "Git ref; SHA/tag recommended for reproducibility. Only valid with git." },
              "path": { "type": "string", "description": "Local directory referenced in place; mutually exclusive with git." }
            },
            "oneOf": [
              {
                "required": ["git"],
                "not": { "required": ["path"] }
              },
              {
                "required": ["path"],
                "not": {
                  "anyOf": [
                    { "required": ["git"] },
                    { "required": ["ref"] }
                  ]
                }
              }
            ],
            "additionalProperties": false
          },
          "optional": {
            "type": "boolean",
            "description": "If true, an entry whose declared directory is missing is dropped from the plan instead of failing the plan. The directory checked is the one declared by the runner the plan selected, falling back to source.path when that runner declares none; the skip is reported as a warning on every execution path. Only a missing directory is tolerated: an optional entry naming an undeclared or unresolvable runner still fails the plan (TASK-319, TASK-374)"
          },
          "primary": {
            "type": "boolean",
            "description": "If true, marks this compose entry as the primary compose entry. Only one entry should have primary=true (TASK-319)"
          },
          "tags": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Tags for categorizing this lifecycle entry"
          },
          "exports": {
            "type": "object",
            "description": "Environment variables to export to subsequent lifecycle entries",
            "additionalProperties": { "type": "string" }
          },
          "health_checks": {
            "type": "object",
            "description": "Health checks specific to this lifecycle entry",
            "patternProperties": {
              "^[\\w\\-.]+$": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "type": { "type": "string", "enum": ["http", "tcp", "command"] },
                  "url": { "type": "string" },
                  "address": { "type": "string" },
                  "command": { "type": "string" },
                  "start": { "type": "string" },
                  "start_hint": { "type": "string" },
                  "timeout": { "type": "integer" },
                  "ready_timeout": { "type": "integer" }
                },
                "required": ["type"]
              }
            },
            "additionalProperties": false
          },
          "process": {
            "type": "object",
            "description": "Local process plugin configuration",
            "additionalProperties": false,
            "properties": {
              "command": { "type": "string", "description": "Command to run" },
              "dir": { "type": "string", "description": "Working directory" },
              "ready_timeout": { "type": "integer", "description": "Timeout in seconds to wait for readiness" }
            }
          },
          "script": {
            "type": "object",
            "description": "Shell script plugin configuration",
            "additionalProperties": false,
            "properties": {
              "up": { "type": "string", "description": "Script to run on up" },
              "down": { "type": "string", "description": "Script to run on down" },
              "stop": { "type": "string", "description": "Script to run on stop" }
            }
          },
          "docker": {
            "type": "object",
            "description": "Standalone Docker container plugin configuration",
            "additionalProperties": false,
            "properties": {
              "image": { "type": "string" },
              "name": { "type": "string" },
              "ports": { "type": "array", "items": { "type": "string" } },
              "volumes": { "type": "array", "items": { "type": "string" } },
              "env": { "type": "object", "additionalProperties": { "type": "string" } },
              "options": { "type": "array", "items": { "type": "string" } }
            }
          },
          "kubectl": {
            "type": "object",
            "description": "Kubectl apply plugin configuration",
            "additionalProperties": false,
            "properties": {
              "manifests": { "type": "array", "items": { "type": "string" } },
              "namespace": { "type": "string" },
              "context": { "type": "string" },
              "kubeconfig": { "type": "string", "description": "Path to kubeconfig file" }
            }
          },
          "helm": {
            "type": "object",
            "description": "Helm chart deployment plugin configuration",
            "additionalProperties": false,
            "properties": {
              "chart": { "type": "string" },
              "release": { "type": "string" },
              "namespace": { "type": "string" },
              "context": { "type": "string" },
              "values": { "type": "array", "items": { "type": "string" } },
              "set": { "type": "object", "additionalProperties": { "type": "string" } }
            }
          },
          "kustomize": {
            "type": "object",
            "description": "Kustomize overlay plugin configuration",
            "additionalProperties": false,
            "properties": {
              "dir": { "type": "string" },
              "namespace": { "type": "string" },
              "context": { "type": "string" }
            }
          },
          "tilt": {
            "type": "object",
            "description": "Tilt local dev plugin configuration",
            "additionalProperties": false,
            "properties": {
              "dir": { "type": "string" },
              "args": { "type": "array", "items": { "type": "string" } }
            }
          },
          "skaffold": {
            "type": "object",
            "description": "Skaffold pipeline plugin configuration",
            "additionalProperties": false,
            "properties": {
              "config": { "type": "string" },
              "profile": { "type": "string" },
              "args": { "type": "array", "items": { "type": "string" } }
            }
          },
          "podman_compose": {
            "type": "object",
            "description": "Podman Compose plugin configuration",
            "additionalProperties": false,
            "properties": {
              "files": { "type": "array", "items": { "type": "string" } },
              "project_name": { "type": "string" }
            }
          },
          "vagrant": {
            "type": "object",
            "description": "Vagrant VM plugin configuration",
            "additionalProperties": false,
            "properties": {
              "dir": { "type": "string" },
              "machine": { "type": "string" }
            }
          },
          "sam": {
            "type": "object",
            "description": "AWS SAM local plugin configuration",
            "additionalProperties": false,
            "properties": {
              "template": { "type": "string" },
              "port": { "type": "integer" },
              "args": { "type": "array", "items": { "type": "string" } }
            }
          },
          "serverless": {
            "type": "object",
            "description": "Serverless offline plugin configuration",
            "additionalProperties": false,
            "properties": {
              "dir": { "type": "string" },
              "port": { "type": "integer" },
              "args": { "type": "array", "items": { "type": "string" } }
            }
          },
          "multipass": {
            "type": "object",
            "description": "Multipass VM plugin configuration",
            "additionalProperties": false,
            "properties": {
              "name": { "type": "string" },
              "image": { "type": "string" },
              "cpus": { "type": "integer" },
              "memory": { "type": "string" },
              "disk": { "type": "string" },
              "cloud_init": { "type": "string" }
            }
          }
        }
      },
      "examples": [
        {
          "compose": {
            "default_runner": "compose",
            "runners": {
              "compose": {
                "files": ["compose.yml"],
                "project_name": "myapp"
              }
            }
          },
          "k8s": {
            "plugin": "kubectl",
            "order": 20,
            "namespace": "myapp-dev"
          }
        }
      ]
    },
    "interaction": {
      "type": "object",
      "description": "Defines the commands and their configurations",
      "patternProperties": {
        "^[\\w\\-.:/\\s]+$": {
          "$ref": "#/definitions/interaction_command"
        }
      },
      "additionalProperties": false
    },
    "provision": {
      "type": "object",
      "description": "Execute commands within provision section. Keys represent provision profiles (e.g., 'default', 'reset'), each containing an array of commands to execute. Use 'default_profile' to set the default profile name.",
      "properties": {
        "default_profile": {
          "type": "string",
          "description": "Name of the default provision profile to use when none is specified",
          "examples": ["setup", "default"]
        }
      },
      "additionalProperties": {
        "type": "array",
        "items": { "$ref": "#/definitions/provision_item" },
        "description": "Named provision profile: list of step/legacy/raw commands"
      },
      "examples": [
        {
          "default": [
            { "step": "Installing dependencies", "run": "bundle install" },
            { "step": "Setting up database", "run": ["rails db:create", "rails db:migrate"] },
            { "step": "Complete", "note": "Run 'rails server' to start" }
          ],
          "legacy_string": [
            "echo 'Setting up...'",
            "docker compose down --volumes",
            "sleep 2"
          ],
          "legacy_structured": [
            { "echo": "🚀 Starting..." },
            { "cmd": "docker compose up -d" }
          ]
        }
      ]
    },
    "environment": {
      "$ref": "#/definitions/environment_vars"
    },
    "env_file": {
      "$ref": "#/definitions/env_file"
    },
    "env_bridge": {
      "type": "object",
      "description": "Gates the 'dva config env seal' and 'dva config env show' commands, both off by default. Valid only at the config root: a module or override file declaring this is rejected, and an imported subproject's own declaration has no effect on the parent. Declaring this section requires 'version' to be at least the release that introduced it. See USAGE.md, 'config env'.",
      "additionalProperties": false,
      "properties": {
        "allow_seal": {
          "type": "boolean",
          "default": false,
          "description": "Enables 'dva config env seal', which bootstraps a new encrypted source from an existing plaintext env_file target. It never overwrites an existing source."
        },
        "allow_show": {
          "type": "boolean",
          "default": false,
          "description": "Enables 'dva config env show', which prints a decrypted env_file entry to the controlling terminal only. Refused in a detected agent environment and when --json is passed."
        }
      },
      "examples": [{ "allow_seal": true, "allow_show": false }]
    },
    "modules": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Paths to module configuration files",
      "examples": [["production"]]
    },
    "infra": {
      "type": "object",
      "description": "Contains infrastructure services configuration",
      "additionalProperties": false,
      "patternProperties": {
        "^[\\w\\-.:/\\s]+$": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "git": {
              "type": "string",
              "pattern": "^(git@|git://|https?://)[\\w\\d.@:/\\-]+$",
              "description": "Git repository URL for the infrastructure component",
              "examples": ["https://github.com/mycompany/redis-config.git"]
            },
            "ref": {
              "type": "string",
              "description": "Specifies the Git reference (branch, tag, or commit) to use",
              "examples": ["main"]
            },
            "path": {
              "type": "string",
              "description": "Local path to the infrastructure component",
              "examples": ["./infra/elasticsearch"]
            }
          },
          "oneOf": [
            { "required": ["git"] },
            { "required": ["path"] }
          ]
        }
      }
    },
    "subprojects": {
      "type": "object",
      "description": "Defines sub-projects that can be integrated when running DVA from a devbox (parent project)",
      "patternProperties": {
        "^[\\w\\-.]+$": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "path": {
              "type": "string",
              "description": "Relative or absolute path to the sub-project directory. Declarations with no import entries, including import: {}, may point at subprojects that will be initialized later; non-empty imports and direct subproject runs require the child path to contain dva.yml.",
              "examples": ["cwrapper-engine-py", "../other-project"]
            },
            "import": {
              "type": "object",
              "description": "What to import from this subproject",
              "additionalProperties": false,
              "properties": {
                "plans": {
                  "type": "array",
                  "description": "Plan names to import from this subproject",
                  "items": {
                    "oneOf": [
                      { "type": "string" },
                      {
                        "type": "object",
                        "properties": {
                          "name": { "type": "string", "description": "Plan name in the subproject" },
                          "as": { "type": "string", "description": "Alias name in the parent namespace" }
                        },
                        "required": ["name"],
                        "additionalProperties": false
                      }
                    ]
                  }
                },
                "interactions": {
                  "type": "array",
                  "description": "Interaction names to import",
                  "items": {
                    "oneOf": [
                      { "type": "string" },
                      {
                        "type": "object",
                        "properties": {
                          "name": { "type": "string" },
                          "as": { "type": "string" }
                        },
                        "required": ["name"],
                        "additionalProperties": false
                      }
                    ]
                  }
                },
                "provision": {
                  "type": "array",
                  "description": "Provision profile names to import",
                  "items": {
                    "oneOf": [
                      { "type": "string" },
                      {
                        "type": "object",
                        "properties": {
                          "name": { "type": "string" },
                          "as": { "type": "string" }
                        },
                        "required": ["name"],
                        "additionalProperties": false
                      }
                    ]
                  }
                }
              }
            },
            "exclude_tags": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Tags to exclude when integrating this sub-project from the parent devbox",
              "examples": [["infra"], ["infra", "monitoring"]]
            }
          },
          "required": ["path"]
        }
      },
      "additionalProperties": false
    },
    "devcontainer": {
      "type": "object",
      "description": "DevContainer configuration for VSCode integration",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Enable or disable devcontainer integration",
          "examples": [true]
        },
        "name": {
          "type": "string",
          "description": "Display name for the devcontainer",
          "examples": ["My Rails App Development"]
        },
        "image": {
          "type": "string",
          "description": "Docker image to use (alternative to dockerFile)",
          "examples": ["mcr.microsoft.com/devcontainers/ruby:3.2"]
        },
        "dockerFile": {
          "type": "string",
          "description": "Path to Dockerfile (alternative to image)",
          "examples": ["Dockerfile", ".devcontainer/Dockerfile"]
        },
        "service": {
          "type": "string",
          "description": "Docker Compose service to use",
          "examples": ["app", "web"]
        },
        "workspaceFolder": {
          "type": "string",
          "description": "Path to workspace folder inside container",
          "examples": ["/workspace", "/app"]
        },
        "remoteUser": {
          "type": "string",
          "description": "User to run as inside container",
          "examples": ["vscode", "node", "root"]
        },
        "features": {
          "type": "object",
          "description": "DevContainer features to install",
          "additionalProperties": {
            "type": "object"
          },
          "examples": [{
            "ghcr.io/devcontainers/features/docker-in-docker:2": {},
            "ghcr.io/devcontainers/features/github-cli:1": {"version": "latest"}
          }]
        },
        "customizations": {
          "type": "object",
          "description": "Tool-specific customizations (VSCode, etc.)",
          "additionalProperties": true,
          "examples": [{
            "vscode": {
              "extensions": ["rebornix.ruby", "castwide.solargraph"],
              "settings": {"ruby.rubocop.path": "/usr/local/bundle/bin/"}
            }
          }]
        },
        "forwardPorts": {
          "type": "array",
          "items": {
            "type": "integer"
          },
          "description": "Ports to forward from container to host",
          "examples": [[3000, 5432, 6379]]
        },
        "postCreateCommand": {
          "oneOf": [
            {"type": "string"},
            {"type": "array", "items": {"type": "string"}}
          ],
          "description": "Command to run after container is created",
          "examples": ["bundle install && rails db:setup"]
        },
        "postStartCommand": {
          "oneOf": [
            {"type": "string"},
            {"type": "array", "items": {"type": "string"}}
          ],
          "description": "Command to run each time container starts",
          "examples": ["rails server"]
        },
        "postAttachCommand": {
          "oneOf": [
            {"type": "string"},
            {"type": "array", "items": {"type": "string"}}
          ],
          "description": "Command to run after attaching to container",
          "examples": ["echo 'Welcome to development environment'"]
        },
        "mounts": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Additional volume mounts",
          "examples": [["source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"]]
        },
        "runArgs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Additional arguments for docker run",
          "examples": [["--cap-add=SYS_PTRACE", "--security-opt", "seccomp=unconfined"]]
        }
      }
    },
    "default_mode": {
      "type": "string",
      "description": "Mode applied automatically when dva up/down/stop/restart is called without --mode/-M. Should reference a minimal infrastructure mode (e.g., 'infra') so that dva up starts only core services by default. Use 'dva up -M full-stack' for everything.",
      "examples": ["infra", "dev"]
    },
    "default_plan": {
      "type": "string",
      "description": "Plan applied automatically when dva up/down/stop/restart is called without naming a plan. References a plans entry. When exactly one plan is defined it is the default automatically; set this to choose the default among multiple plans (e.g. 'dev').",
      "examples": ["dev", "local-dev"]
    },
    "suggestion_ignore": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Glob patterns for Makefile/package.json targets to suppress from config suggestion warnings. Use when targets are intentionally not mapped to DVA interactions (e.g., CI-only targets, aliases covered by other interactions).",
      "examples": [["*-release", "clippy*", "test-e2e-*"]]
    },
    "modes": {
      "type": "object",
      "description": "Named operational modes for dva up (--mode/-M flag). Each mode defines which compose profiles/services to start, health checks to run, and environment overrides.",
      "patternProperties": {
        "^[\\w\\-.]+$": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "description": {
              "type": "string",
              "description": "Human-readable description of this mode"
            },
            "compose_profiles": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Docker Compose profiles to activate (passed as --profile flags)",
              "examples": [["infra"], ["infra", "monitoring"]]
            },
            "compose_services": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Explicit list of compose services to start. Empty array = skip compose entirely (native mode). Omit = start all services.",
              "examples": [["postgres", "redis"], []]
            },
            "health_checks": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Health check names to activate (from health_checks section). Omit = use all.",
              "examples": [["app", "worker"]]
            },
            "endpoint_tags": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Show only endpoints matching these tags. Omit = show all endpoints.",
              "examples": [["app"], ["app", "monitoring"]]
            },
            "environment": {
              "$ref": "#/definitions/environment_vars"
            },
            "stack": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Stack entry names to include in this mode. Only these entries will be started.",
              "examples": [["compose", "kubectl"]]
            },
            "provision": {
              "type": "string",
              "description": "Provision profile to suggest when services start for the first time",
              "examples": ["setup", "reset"]
            },
            "build": {
              "type": "string",
              "description": "Build strategy for this mode: \"docker\" (compose build), \"native\" (run interaction.build.replace steps; errors if undefined), or a custom shell command. Read by the build runner (compose.go).",
              "examples": ["native", "docker", "make build"]
            },
            "run": {
              "type": "string",
              "description": "Run strategy for this mode: \"docker\" (compose up), \"native\" (process via health_checks.start), or a custom shell command.",
              "examples": ["native", "docker"]
            }
          }
        }
      },
      "additionalProperties": false,
      "examples": [{
        "infra-only": {
          "description": "Infrastructure only",
          "compose_services": ["postgres", "redis"]
        },
        "hybrid": {
          "description": "Infra in containers, app locally",
          "compose_services": ["postgres", "redis"],
          "health_checks": ["app"],
          "endpoint_tags": ["app"],
          "environment": { "DATABASE_URL": "postgres://localhost:5432/mydb" }
        },
        "native": {
          "description": "Everything local, no Docker",
          "compose_services": [],
          "health_checks": ["postgres", "redis", "app"]
        }
      }]
    },
    "environments": {
      "type": "object",
      "description": "Named environment configurations for --env/-E flag. Each environment defines environment variable overrides.",
      "patternProperties": {
        "^[\\w\\-.]+$": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "description": {
              "type": "string",
              "description": "Human-readable description of this environment"
            },
            "environment": {
              "$ref": "#/definitions/environment_vars"
            },
            "stack": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Stack entry names to include when this environment is active (empty=all)"
            },
            "stack_overrides": {
              "type": "object",
              "description": "Lifecycle entry overrides to apply on top of the base stack when this environment is active",
              "patternProperties": {
                "^[\\w\\-.]+$": {
                  "type": "object"
                }
              }
            }
          }
        }
      },
      "additionalProperties": false,
      "examples": [{
        "dev": {
          "description": "Development settings",
          "stack": ["compose"],
          "environment": { "LOG_LEVEL": "debug", "ENABLE_HOT_RELOAD": "true" }
        },
        "stg": {
          "description": "Staging settings",
          "stack": ["kubectl"],
          "environment": { "DATABASE_URL": "postgres://localhost:5432/mydb_stg" }
        }
      }]
    },
    "ssh": {
      "type": "object",
      "description": "SSH agent configuration",
      "additionalProperties": false,
      "properties": {
        "agent_image": {
          "type": "string",
          "description": "Docker image for the SSH agent container (default: whilp/ssh-agent)",
          "examples": ["whilp/ssh-agent", "my-registry/ssh-agent:latest"]
        }
      }
    },
    "endpoints": {
      "type": "object",
      "description": "Declares user-facing endpoint URLs for the project. Use `source` to reference compose service ports (URL auto-resolved), or `url` for non-compose endpoints. Keys matching health_check names are auto-linked for status display.",
      "patternProperties": {
        "^[\\w\\-.]+$": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "url": {
              "type": "string",
              "description": "User-facing access URL. Required if source is not set.",
              "examples": ["http://localhost:8080", "ssh://git@localhost:2222", "grpc://localhost:50051"]
            },
            "source": {
              "type": "string",
              "description": "Compose service:host_port reference. DVA resolves the URL automatically from compose port mappings. Use instead of url for compose services.",
              "examples": ["postgres:15432", "app:11700", "gitea:11792"]
            },
            "label": {
              "type": "string",
              "description": "Human-readable display name",
              "examples": ["API Server", "Git SSH", "Admin Panel"]
            },
            "tags": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Tags for filtering (used by modes.endpoint_tags). Inherited from service tags under stack.<entry>.runners.compose if omitted.",
              "examples": [["app"], ["infra", "monitoring"]]
            },
            "paths": {
              "type": "object",
              "description": "Sub-paths with descriptions (key: path, value: description)",
              "additionalProperties": { "type": "string" },
              "examples": [{ "/api/v1": "REST API", "/metrics": "Prometheus metrics" }]
            }
          },
          "required": ["label"]
        }
      },
      "additionalProperties": false,
      "examples": [{
        "app-http": {
          "source": "app:11700",
          "label": "App HTTP",
          "tags": ["app"],
          "paths": { "/health": "Health check", "/api/v1": "REST API" }
        },
        "app-ssh": {
          "url": "ssh://git@localhost:2222",
          "label": "Git SSH",
          "tags": ["app"]
        }
      }]
    },
    "health_checks": {
      "type": "object",
      "description": "Health checks for non-compose services (e.g., local processes, external dependencies)",
      "patternProperties": {
        "^[\\w\\-.]+$": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "type": {
              "type": "string",
              "enum": ["http", "tcp", "command"],
              "description": "Health check type"
            },
            "url": {
              "type": "string",
              "description": "URL for http type health check",
              "examples": ["http://localhost:8080/health"]
            },
            "address": {
              "type": "string",
              "description": "Host:port for tcp type health check",
              "examples": ["localhost:5432"]
            },
            "command": {
              "type": "string",
              "description": "Shell command for command type health check",
              "examples": ["pg_isready -U dev"]
            },
            "start": {
              "type": "string",
              "description": "Shell command to auto-start this service in the background",
              "examples": ["redis-server --daemonize yes", "postgres -D /usr/local/var/postgresql@14"]
            },
            "start_hint": {
              "type": "string",
              "description": "Human-readable hint shown when service is not ready (no auto-start)",
              "examples": ["brew services start postgresql"]
            },
            "timeout": {
              "type": "integer",
              "description": "Timeout in seconds (default: 2)",
              "default": 2,
              "examples": [5]
            },
            "ready_timeout": {
              "type": "integer",
              "description": "Maximum time in seconds to wait for a service to become ready after auto-start (default: 30)",
              "default": 30,
              "examples": [60]
            }
          },
          "required": ["type"]
        }
      },
      "additionalProperties": false
    },
    "ci": {
      "type": "object",
      "description": "Named CI gate profiles. Every step is explicit and dependencies define their execution order.",
      "additionalProperties": false,
      "properties": {
        "profiles": {
          "type": "object",
          "patternProperties": {
            "^[A-Za-z0-9][A-Za-z0-9_.-]*$": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "description": { "type": "string" },
                "timeout": { "type": "string", "description": "Positive Go duration for the whole profile" },
                "warn_after": { "type": "string", "description": "Positive Go duration after which the profile warns" },
                "max_parallel": { "type": "integer", "minimum": 1, "maximum": 32 },
                "locks": {
                  "type": "array",
                  "description": "Named shared resources that must be exclusive across local CI runs",
                  "items": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*$" }
                },
                "steps": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "additionalProperties": false,
                    "properties": {
                      "name": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*$" },
                      "run": { "type": "string" },
                      "depends_on": { "type": "array", "items": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*$" } },
                      "workdir": { "type": "string" },
                      "environment": { "type": "object", "additionalProperties": { "type": "string" } },
                      "timeout": { "type": "string", "description": "Positive Go duration no greater than the profile timeout" }
                    },
                    "required": ["name", "run"]
                  }
                }
              }
            }
          },
          "additionalProperties": false
        }
      },
      "required": ["profiles"]
    },
    "checks": {
      "type": "array",
      "description": "Environment checks for 'dva doctor'. Validates prerequisites before running services.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string",
            "description": "Human-readable name of the check",
            "examples": ["Docker socket accessible"]
          },
          "type": {
            "type": "string",
            "enum": ["file_exists", "command", "docker_socket"],
            "description": "Type of check to perform"
          },
          "path": {
            "type": "string",
            "description": "File path to check (for file_exists type)",
            "examples": [".env", "docker-compose.yml"]
          },
          "command": {
            "type": "string",
            "description": "Shell command to run (for command type, exit 0 = pass)",
            "examples": ["docker info > /dev/null 2>&1"]
          },
          "fix_hint": {
            "type": "string",
            "description": "Suggestion shown when the check fails",
            "examples": ["cp .env.example .env", "dva provision setup"]
          },
          "fix": {
            "type": "string",
            "description": "Shell command executed by `dva doctor --fix` to auto-remediate a failed check. Runs via `sh -c` from the config directory; only used when --fix is passed.",
            "examples": ["cp .env.example .env", "touch .sentinel"]
          }
        },
        "required": ["name", "type"]
      },
      "examples": [
        [
          { "name": "Docker socket accessible", "type": "docker_socket", "fix_hint": "Start Docker Desktop" },
          { "name": ".env file exists", "type": "file_exists", "path": ".env", "fix_hint": "cp .env.example .env", "fix": "cp .env.example .env" },
          { "name": "DB migrations applied", "type": "command", "command": "make migrate-status 2>&1 | grep -qv pending", "fix_hint": "dva provision setup" }
        ]
      ]
    }
  }
}
